{
  "name": "blog",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.16.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pages:preview": "npm run build && npx wrangler pages dev dist",
    "pages:deploy": "npm run build && npx wrangler pages deploy"
  },
  "devDependencies": {
    "astro": "^6.1.4"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.0"
  }
}
